name: HonKit Test

on:
  push:
    paths:
      - '**.md'
      - 'SUMMARY.md'
      - 'README.md'
      - 'package.json'
  pull_request:
    paths:
      - '**.md'
      - 'SUMMARY.md'
      - 'README.md'
      - 'package.json'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Install dependencies
        run: npm install

      - name: 📚 Build HonKit locally
        run: npx honkit build

      - name: 🔗 Check internal links (optional)
        run: npx honkit serve & npx markdown-link-check README.md --quiet

      # optional: grammar/spell check step if you install those tools
